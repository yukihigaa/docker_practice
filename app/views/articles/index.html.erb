<div class="container">
    <div class="row">
        <% @articles.each do |article| %>
            <div class="col-md-3">
                <%= article.title %>
                <%= article.created_at.strftime('%Y年%m月%d日 %H:%M') %>
            </div>
        <% end %>
    </div>
</div>